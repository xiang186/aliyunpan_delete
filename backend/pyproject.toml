[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aliyundrive-duplicate-cleaner-backend"
version = "0.1.0"
description = "Backend service for AliyunDrive duplicate file cleaner"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn[standard]==0.29.0",
    "sqlalchemy==2.0.30",
    "pydantic==2.7.1",
    "pydantic-settings==2.2.1",
    "httpx==0.27.0",
    "cryptography==42.0.7",
    "sse-starlette==2.1.0",
    "python-dotenv==1.0.1",
    "aiosqlite==0.20.0",
]

[project.optional-dependencies]
dev = [
    "pytest==8.2.0",
    "pytest-asyncio==0.23.6",
    "hypothesis==6.100.1",
    "httpx==0.27.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["app"]
